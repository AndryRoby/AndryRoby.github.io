/* ARLing session recording is disabled (A-088, 2026-09-19).
 *
 * Keep this inert entry point for pages that still reference /zaznam.js.
 * The unfinished recorder must not read storage, create identifiers,
 * attach listeners, load a library or send requests.
 * Aggregate analytics has its own script and is not changed here.
 * Re-enabling requires a separately verified implementation and privacy text.
 */
'use strict';
